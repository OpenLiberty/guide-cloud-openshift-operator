apiVersion: v1
kind: BuildConfig
metadata:
  name: "inventory-bc"
  labels:
    build: "inventory-bc"
spec:
  source:
    binary: {}
    type: Binary
  strategy:
    type: Docker
  output:
    to:
      kind: ImageStreamTag
      name: "inventory-is:latest"